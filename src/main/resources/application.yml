server:
  port: 5001
  error:
    include-stacktrace: never

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/brokerwallet_comm
    username: root
    password: sirius

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

tencent:
  cos:
    # 腾讯云账号
    secretId: AKID4474rRq49MPJ3gAVgibRNG5nT0Pgu6C6
    secretKey: lLl270g8wZ0EF7oNm03vb7TedUBh198x
    # COS 地域
    region: ap-guangzhou
    # 存储桶名称
    bucketName: wallet-community-1416742399
    # 临时密钥有效期，单位秒，最小900s
    durationSeconds: 1800

# 连接超时配置
timeout:
  connect: 30     # 连接超时(秒)
  read: 120       # 读取超时(秒)
  write: 120      # 写入超时(秒)

# 日志配置
logging:
  level:
    org.web3j: INFO
    com.brokerwallet.blockchain: DEBUG

jwt:
  secret: bXktc3VwZXItc2VjcmV0LWtleS0zMmJ5dGVzLWxvbmc=
  expire: 604800   # 7天（单位：秒）
